#### npm install
```
npm install //或
cnpm install
```
### run
```
npm run c page //page代表页面的命名，执行后会在/src/pages/目录下自动创建page文件夹，并且在page文件夹里创建index.html，index.js ，index.scss  添加page页面之后 重新执行run dev 
npm run dev //开发环境
npm run build //生产环境打包
npm run build --report //打包后文件大小分析命令
```
