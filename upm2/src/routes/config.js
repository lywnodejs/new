
const PREFIX = '/upm2-static';

const MAIN = `${PREFIX}/main`;
const MANAGE = `${PREFIX}/manage`;
const BUSINESSMANAGE = `${PREFIX}/businessmanage`;
const MOB = `${PREFIX}/mob`;

export {
  PREFIX,
  MAIN, MANAGE, BUSINESSMANAGE, MOB
};
